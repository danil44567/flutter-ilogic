**[🏠 Вернуться на главную](README.md)**

Для решения заданий воспользуйтесь:

**[Руководство по работе с API во Flutter](api_guide.md)** (рекомендуется изучить перед началом)

---

## Задания по работе с Kinopoisk Unofficial API

### Важные замечания перед началом

Документация к API доступна по адресу: **https://kinopoiskapiunofficial.tech/documentation/api/**

Для использования API необходимо:
1. Зарегистрироваться на сайте https://kinopoiskapiunofficial.tech/
2. Получить персональный API-ключ (токен).
3. Добавлять в каждый запрос заголовки:
   ```dart
   headers: {
     'X-API-KEY': 'ваш_токен',
     'Content-Type': 'application/json',
   }
   ```

Базовый URL: `https://kinopoiskapiunofficial.tech/api/`

Все запросы выполняйте с помощью пакета `http`. Обязательно обрабатывайте состояния загрузки и ошибки.

---

### Задание 1

Реализуйте поиск фильмов по ключевому слову.

- Создайте страницу с `TextField` и кнопкой "Поиск".
- При нажатии выполняйте GET-запрос: `/v2.1/films/search-by-keyword?keyword=ваше_слово&page=1`
- Отобразите список результатов в `ListView`: название фильма (на русском и оригинальное), год выпуска, постер (если есть).
- Используйте `Image.network` для постера.

### Задание 2

Создайте страницу с популярными фильмами.

- Выполните GET-запрос: `/v2.2/films/top?type=TOP_250_BEST_FILMS&page=1`
- Отобразите первые 20 фильмов в `ListView`.
- Для каждого фильма покажите: постер, название, рейтинг (ratingKinopoisk), год и жанры.

### Задание 3

Реализуйте детальную страницу фильма.

- На странице из Задания 1 или 2 добавьте возможность перехода на детальную страницу по нажатию на элемент списка (передайте ID фильма).
- На новой странице выполните GET-запрос: `/v2.2/films/{filmId}`
- Отобразите: большой постер, название, слоган, описание, страну, жанры, длительность, рейтинг и возрастной рейтинг.

### Задание 4

Добавьте отображение актёров фильма.

- На детальной странице фильма (из Задания 3) выполните дополнительный запрос: `/v1/staff?filmId={filmId}`
- Отобразите список актёров в горизонтальном `ListView` или `GridView`: фото актёра (если есть), имя и роль (description).

### Задание 5

Реализуйте поиск похожих фильмов.

- На детальной странице фильма добавьте раздел "Похожие фильмы".
- Выполните GET-запрос: `/v2.2/films/{filmId}/similars`
- Отобразите список похожих фильмов (постер + название) с возможностью перехода на их детальные страницы.

### Задание 6

Создайте страницу с премьерами месяца.

- Выполните GET-запрос: `/v2.2/films/premieres?year=2025&month=DECEMBER` (изменяйте год и месяц по необходимости).
- Отобразите список премьер в `GridView` или `ListView`: постер, название, страны и жанры.

### Задание 7*

Добавьте отображение сезонов и серий для сериалов.

- Определите, является ли фильм сериалом (по полю `type` или наличию сезонов).
- Если да — выполните запрос: `/v2.2/films/{filmId}/seasons`
- Отобразите список сезонов, а при выборе сезона — список серий с названиями и описаниями.

### Задание 8*

Реализуйте фильтрацию фильмов по жанру и стране.

- Сначала получите списки жанров и стран: `/v2.2/films/filters`
- Создайте страницу с выпадающими списками (DropdownButton) для выбора жанра и страны.
- При нажатии "Поиск" выполните запрос: `/v2.2/films?genres=1&countries=2&page=1` (используйте соответствующие ID).
- Отобразите результаты поиска.

### Задание 9*

Добавьте кэширование изображений и обработку отсутствия интернета.

- Используйте пакет `cached_network_image` для постеров (добавьте в pubspec.yaml).
- Добавьте индикатор загрузки и сообщение об ошибке при отсутствии сети.

**[🏠 Вернуться на главную](README.md)**
